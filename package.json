{
  "name": "pokelogger",
  "private": true,
  "type": "module",
  "description": "Local-only Pokémon EV training tracker built with native Web Components.",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\""
  }
}
